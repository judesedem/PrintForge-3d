# PrintForge-RN Final Frontend

Latest cumulative Expo Router + TypeScript restyle.

## Run

```bash
npm install
cp .env.example .env
# Add your Firebase and Google values
npx expo start
```

The real `.env` and all secrets were removed before packaging.

This is a styling/navigation update. Backend API integration was not rewritten.
